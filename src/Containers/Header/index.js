import React from 'react'

import classes from './Header.scss'

export default () => (
    <div className={classes.container}>
        <div className={classes.content}>
            <h2 className={classes.heading}>This template is awesome</h2>
            <div className={classes["sub-heading"]}>resto restaurant home page website template</div>
        </div>
    </div>
)